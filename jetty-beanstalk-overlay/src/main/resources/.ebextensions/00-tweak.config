#
# this config is purely to enable console access to generally protected and private
# elastic beanstalk configuration
#
commands:
  a_permissions:
    command: chmod -R o+r elasticbeanstalk
    cwd: /opt
  b_permissions:
    command: chmod -R o+w elasticbeanstalk
    cwd: /opt